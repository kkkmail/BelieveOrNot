// js/actions/selectChallengeCard.js
// This function is no longer needed as challenge card selection is handled in handlePreviousCardClick
// Keeping empty function to prevent import errors

export function selectChallengeCard(index) {
    console.log("selectChallengeCard function no longer needed - handled in handlePreviousCardClick");
}